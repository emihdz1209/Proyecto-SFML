#ifndef SELECTIONSORT_H
#define SELECTIONSORT_H

#include <SFML/Graphics.hpp>
#include <vector>

void SelectionSort(std::vector<int> &arr, sf::RenderWindow &window, sf::Text &text, bool &sorting, sf::Font &font);

#endif // SELECTIONSORT_H